{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h2>Connexion</h2>
        {{ form_row(form.email) }} 
        {{ form_row(form.password) }}
        {{ form_end(form) }}
    </div>
{% endblock %}